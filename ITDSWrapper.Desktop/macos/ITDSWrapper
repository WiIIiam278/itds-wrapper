#!/bin/sh

DIR=$(dirname "$0")
ARM64=$(sysctl -ni hw.optional.arm64)

if [[ "$ARM64" == 1 ]]; then
    exec "$DIR/Into the Dream Spring.ARM64.app/Contents/MacOS/ITDSWrapper.Desktop"
else
    exec "$DIR/Into the Dream Spring.X64.app/Contents/MacOS/ITDSWrapper.Desktop"
fi